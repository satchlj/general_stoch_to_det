import stoch_to_det.MainTheorems

/-!
# general_stoch_to_det — root module

The public endpoint `stoch_to_det.general_stoch_to_det` proves an
alphabet-size-independent stochastic-to-deterministic comparison for every
finite coordinate law with `n >= 3`.
-/
